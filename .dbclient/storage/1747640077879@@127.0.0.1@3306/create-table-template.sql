CREATE TABLE table_name(  
    id INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    content TEXT
);

-- C.R.U.D


-- S.Q.L -> Structured query Language (used to manager relational dbs)

/**
* should default using small case
* should use snake case naming convention
*/


CREATE TABLE products (
    id INTEGER,
    name TEXT,
    price INTEGER,
    quantity INTEGER,
    discount INTEGER,
    
)